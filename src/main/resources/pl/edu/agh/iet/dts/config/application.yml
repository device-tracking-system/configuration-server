server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/device-tracking-system/configuration-server
        native:
          search-locations: classpath:pl/edu/agh/iet/dts/config/services/
  profiles:
    active: native
